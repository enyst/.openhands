You can use remote gh from your env to work with github, because it is actually a classic github token.

Remember to NOT use `set -e` or any other `set` in this runtime environment, because it bricks it.
You can use the terminal commands directly, that works fine.
